# AGENTS.md

本知识库在资料入库、查询、归档和质量检查时使用已安装的 Agent Skill `karpathy-llm-wiki`。指令审阅、脚本维护等元工作按各自任务执行，不自动触发 Ingest 或改写知识文章。本文件只放每次都需要载入的本地规则；细节按需阅读 `references/` 和 `templates/`。

## 项目目标

本库用于追踪清华前沿科技人才、奖项/竞赛、院系/实验室、创业信号和公司之间的关系。重点领域包括 AI、具身智能/机器人、脑机接口、核聚变/先进能源，以及明显受益于 AI 的交叉学科。

## 本地结构

- `raw/`：不可变原始资料，只新增，不改写原意。
- `wiki/`：编译后的知识文章，使用一层主题目录：`wiki/<topic>/<article>.md`。
- `wiki/index.md`：全局索引。
- `wiki/log.md`：追加式操作日志。
- `templates/`：本库页面模板，不属于 wiki 文章。
- `references/`：按需查询的本地细则，不要每次全量载入。

当前 wiki 主题目录：

- `wiki/universities/`：已纳入覆盖范围的高校总览；具体层级规则见 `references/entity-and-page-rules.md`。
- `wiki/maps/`：观察池、已创业地图、奖项入口、方向地图。
- `wiki/people/`：教授、学生、校友、创业者、产业研究员。
- `wiki/orgs/`：院系、研究院、实验室、课题组。
- `wiki/awards/`：奖项和竞赛，保留独立页。
- `wiki/companies/`：清华关联公司和创业项目。
- `wiki/questions/`：冲突、不确定性和待验证问题。

## 每次都要遵守

- 主要用中文写作；英文官方名称、英文简称、论文/项目术语保留原文。
- 库内文件使用标准 Markdown 相对链接，不把 Obsidian `[[wikilink]]` 作为主链接格式；对话中的本地文件链接按宿主要求使用绝对路径。
- 人物、组织、公司等实体页只为达到建页阈值的稳定实体创建；地图、问题页和用户明确要求的回答归档是允许的页面类型。新闻、论文、专利、融资事件、任职事件默认作为来源或实体页中的事件，不建独立页。
- 奖项/竞赛页保留独立页面，用于承接长期覆盖和 T2/T3 长尾人才线索。
- 关键判断必须能追溯证据；不确定信息写成待验证，不写成结论。
- 禁止记录私人手机号、私人邮箱、住址、身份证、家庭成员、非公开群聊、朋友圈、内部通讯录、未授权简历全文等敏感或非公开信息。


## 何时读取按需文件

- 创建或升级人物/组织/公司/奖项页面：读 `references/entity-and-page-rules.md`，再套用对应 `templates/tmpl-*.md`。
- 维护奖项、竞赛、观察池覆盖度：读 `references/award-competition-workflow.md`。
- 判断来源等级、置信度、隐私边界：读 `references/source-and-privacy-policy.md`。
- 命名文件、写相对链接、更新索引和日志：读 `references/naming-links-and-index.md`。
- 选择首批入口或方向覆盖范围：读 `references/priority-entry-list.md`。
- 需要完整项目规划背景时：读 `tsinghua_frontier_talent_wiki_plan.md`。

## 工作流

- Ingest：按 `karpathy-llm-wiki`，先保存到 `raw/`，再编译或合并到 `wiki/`，随后更新 `wiki/index.md` 和 `wiki/log.md`。
- Query：先读 `wiki/index.md` 定位文章，再读相关页面回答；普通查询不写文件。
- Archive：只有用户明确要求保存回答时，才归档为新的 wiki 页面，并更新索引和日志。
- Lint：可自动修复确定性问题；事实矛盾、陈旧结论、孤立页面等启发式问题只报告，除非用户要求修改。


## 适用边界与完成标准

- 同一事项优先使用本库具体规则和模板；通用 wiki 技能补充流程，不要求重复套用两套模板。人物建页需同时满足实体规则中的独立信号门槛，T 分级本身不豁免门槛。
- 明确要求导入、研究入库或修复时，连续完成已授权环节，无需在保存 raw、编译、级联更新和维护索引日志之间重复确认。只要求研究或回答时不自动持久化到 wiki。
- 批量任务以用户指定范围为完成边界；关联检索用于发现受当前证据实质影响的文章，不扩展为全库重研。单条来源不可达或证据不足时记录缺口并继续其他条目；不得用推断补造事实。
- Ingest 的完成是选定来源已保存、有效新增内容已合并、实质受影响页面已同步、索引和日志已维护；无新增内容仅保留 raw 并记日志。验证本次链接、元数据和关键证据，已有效验证的内容不重复全量检查。
- 仅请求 Lint 时，确定性问题可修复，事实与启发式问题报告；明确要求修复时，可在证据充分且符合领域规则的范围内一并修复，来源冲突仍需保留争议状态。
- 指令和工具维护的记录放在对应维护产物中，不为元工作制造 raw 来源、wiki 文章或研究日志。
