# 清华大学具身人才地图（HTML）

基于原“研究主线与身份版”矢量图，2026-09-11 同步知识库新增研究人才、清华校友与公司关系。HTML 独立维护，原 PPTX 不随本次更新改写。

- `index.html`：内嵌 SVG 与搜索坐标，可直接本地打开。
- `style.css`、`app.js`：页面外壳（标题、统计、图例、缩放盘、详情抽屉）、搜索、院系定位、缩放拖动、悬停与选中反馈。交付时三个文件放在同一目录。
- 当前包含 175 位主体人物、10 位组织背景人物、45 个实验室标签、79 家公司／平台、103 个公司关联气泡（同名公司可出现在不同人物附近）。
- `source/map_data.json` 保存图谱数据快照，`source/layout.js` 与 `source/build.js` 沿用原布局生成逻辑。修改数据后，在仓库根目录运行 `node talent-map/source/build.js`，同步 SVG、统计和搜索坐标；仅需 Node.js，无额外依赖。此快照不会自动抓取后续 wiki 更新。

2026-09-10 同步：刘松铭—朱军—LiberAI、林凡淇—高阳—LiberAI；姚颂—正行创新、于超—正行创新及汪玉师承；张金涛与刘松铭的首届腾讯青云奖学金信息。来源为对应人物/公司页及其 raw 证据。新公司详情提供公开来源链接。

证据边界：LiberAI Pre-A 轮采用财联社报道，发布日期未明，不把推断月份当作事实；正行创新融资及技术信息注明 DeepTech 单篇报道口径。林凡淇具体学籍待核；高阳的导师关系不表示其参与 LiberAI 创业。深鉴科技与东方空间仍以姚颂历史身份显示；姚颂在正行创新确切职务待核。

Quartz 构建仍由 `site/scripts/publish-talent-map.mjs` 仅复制三个交付文件至 `site/public/talent-map/`。源数据与生成脚本不发布。HTML 随字号优化版统一发布。

## 知识内容同步（2026-09-11）

- 新增13位人物：李忆唐、罗长盛、白雨石、姜汉卿、潘佳、刘希慧、张福民、何奇洸、潘玲、涂锋斌、谢源、訾云龙、张启航。各节点链接对应独立知识页，身份和成果按页内原文证据编译；外校现职与清华学缘分开说明。
- 新增姜汉卿—西湖交互（创办）、潘佳—逐际动力（首席科学家，2023年公告；最新任职待核）；新增弋力—李忆唐（研究指导）、李涓子—白雨石（博士导师）。不由研究指导反推实验室成员，不由技术贡献推定企业任职。
- 李昊展移出正式人物与关系数据，其内部名单线索仍在人工智能学院页的本地待补证小节。图上主体人数为原163人减1人、再加13人。
- VAR采用当前官方名称，EVAR是否为正式旧称在详情中保留待核；MARS补官方名称与代表研究。补VAR、MARS、求之科技的知识页入口。
- 郭钰铎、王同翰注明课题组PI及正式到岗待核；周谷越保留求之科技首席科学家，不升级为创始人。
- 姜汉卿按力学学科历史进入航院入口，明确2001年博士毕业早于2004年建院；訾云龙进入材料科学与工程系历史学缘入口。张福民、何奇洸的清华院系未核，保留在院系待核区。
- 按用户本次要求，高喆的人物数据与SUNIST分组保持不变。用户明确其为SUNIST项目负责人、谭熠的领导；本轮不执行上一轮检查建议的降级，也未改写相关wiki页面。
- 天谋科技、莫测智能、理工华汇继续由已有知识页承接，不制造缺乏具名证据的人物—公司关系。

`groupByMentor: false` 用于已有指导证据但尚无实验室成员证据的人物：保留直接关系线，阻止布局自动把其归入导师实验室。新增卡片只承接历史学缘，不新建知识库实体页。原始资料与wiki文章保持不变；本次维护记录在此保存。

本轮验证：12项关系与布局测试通过；桌面1440×1000、窄屏390×844各58项搜索/详情/关系检查通过，截图人工检查及浏览器控制台无新增问题。300个地图知识页目标均有本地生成页面，地图源HTML与网站副本一致。临时Git索引下整站构建通过，1,554个生成页面、44,231个本地链接/资源检查为0失败。真实发布清单仍有其他任务尚未纳入Git的知识页/来源，因此默认发布门禁仍需在正式发布前处理；本轮未改变真实暂存区、未提交或部署。

## 点击查看直接关系（2026-09-11）

点击人物后，本人、直接相关的人物和公司气泡会突出显示，其他人物与公司淡化。包含已记录的入边和出边，也包含跨院系关系；不会由同实验室、同公司等信息推断二跳关系。培养关系保留导师到学生的箭头，合作、待核和历史参与使用虚线及原始标签。

点击公司会同时突出该公司的所有展示气泡及具名关联人物，保留各处布局与各人的不同角色。例如点击刘松铭只突出朱军和本人旁的 LiberAI；点击 LiberAI 才汇总刘松铭、林凡淇，不展开两人的导师。

搜索结果、画布点击和详情关联按钮共用选中状态。点击空白、关闭详情、按 `Esc`、回到“全图”或“阅读”会清除；缩放拖动保留状态，全图语义档暂时隐藏关系线，回到中近景恢复。窄屏详情在底部展开；缩放按钮避开详情面板。

画布上直接点击节点只更新高亮和详情，保持当前缩放比例及节点在屏幕上的位置。搜索结果、详情关联按钮和“定位院系”属于主动跳转，继续定位目标；这一划分与香港地图一致。

`source/relations.cjs` 从现有 `mentor` 和 `instances` 编译关系，区分公司实体与展示实例；新增公司角色需显式归入对应类型。`source/build.js` 生成带标识的 SVG 节点、关系路径和浏览器索引，关系层与原地图共用 `viewBox`，无需拖动时重新计算端点。现有来源引用按快照保留，不代表每次构建重新核验事实。

修改后在仓库根目录验证：

```bash
node talent-map/source/build.js
node --check talent-map/app.js
node --test talent-map/source/relations.test.cjs
```

随后运行网站既有构建。发布仍为 `index.html`、`style.css`、`app.js` 三件套，无新增浏览器依赖。

## 视觉与交互改版（2026-09-10）

数据、院系列位、实验室分组和关系口径均未改动；改的是渲染与页面外壳。画布 85.3 × 64 布局单位（1 单位 = 72 px），即 6141.6 × 4608 px。

**页面外壳外移。** 标题、统计、图例、页脚说明原先画在 SVG 内部，会随缩放一起变小；现在改为 HTML，始终以浏览器字号呈现。SVG 内的深色横幅、"清华大学"中心气泡、图例块和统计行相应删除，顶部空间回收给院系卡片。

**七级字号阶梯。** 11 / 13 / 16 / 20 / 26 / 34 / 44 px，`build.js` 在生成时对阶梯外的字号直接抛错。此前 `text()` 有一条 `Math.max(size,12)` 下限，把 1011 个文本里的 640 个压到同一档，三级信息层次实际失效。

**人物节点改为圆角卡片**（1.89 × 0.92 单位），左侧竖条表示创业状态；公司仍是圆形。形状本身区分类型，姓名与身份各占一行，不再靠气泡直径塞字。

**配色收敛到 12 个 token。** 饱和色只留四种语义：绿=创办、蓝=任职/顾问、琥珀=待核、紫=培养关系；原先按院系区分的 5 种色相取消，院系靠位置和标签区分。描边色从 25 种降到 12 种。

**深色模式。** 所有绘制颜色以 `var(--m-*, #浅色)` 形式输出（2045 处），`style.css` 中一组变量即可整体换肤，跟随 `prefers-color-scheme`，无需重新生成 SVG。

**师承箭头修复。** `line()` 的 `arrow` 参数此前从未接到 `marker-end`，箭头一直没有渲染。现按关系类型输出 `arrow-mentor` / `arrow-pending` / `arrow-none` 三种 marker。跨行连线走行间空隙，从卡片左上角起步，不再绕卡片三边（视觉上像给卡片画了个选中框）。

**语义缩放。** 依当前比例给 `<svg>` 加 `z-far` / `z-mid` / `z-near`：低于 34% 只显示各院系名称与"N 人 · M 位已创业"；低于 70% 显示姓名与实验室结构，隐藏身份、备注与角色；以上显示全部。窄屏（<800px）默认以全图档打开。

**交互。** 悬停出现指示环；选中时聚光罩变暗其余区域，右侧抽屉列出该节点信息，关联实体做成可点按钮，可沿图谱来回跳转；`/` 聚焦搜索，`Esc` 取消选中，`0` 回到全图。缩放盘移到右下角，不再压在图面中央。

## 阅读字号调整（2026-09-10，已被上一节取代）


保留当前数据、院系列位置、实验室分组和关系。姓名由14.5提高至20.3 SVG px，普通公司名由10.8提高至15 px，辅助文字最低12 px；人物气泡直径由0.96提高至1.20布局单位，公司气泡提高至1.0。文字自动换行，角色和说明行按内容分配空间；实验室间距及院系纵向间距收紧约25%。默认以110%从地图左上院系打开，“阅读”恢复此比例，“全图”保留整体预览。浏览器窗口变化不再将用户缩放重置为全图。未修改PPTX。

## 知识库详情入口

详情面板为已建独立页的实体显示“查看知识库页面”，以新标签页打开线上知识库；没有独立页时不显示。人物沿用数据中的人物页路径，公司和组织别名对应保存在 `source/wiki-links.json`。新增独立页或更改对应关系后，核对该映射并运行构建；构建时排除不存在的路径，不把其他文章中的提及当成实体页。院系可通过概览卡片或院系下拉框打开详情。
